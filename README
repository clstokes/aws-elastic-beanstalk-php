I) Introduction 
---------------
This project is meant to show how you can run PHP on Amazon's Elastic Beanstalk service.

It's setup as a Maven project and uses Quercues, a "100% Java implementation of PHP 5," to run PHP in a Tomcat container.


II) Getting Started
-------------------
Run the project root, run 'mvn package' to create the deployable war file. Once complete, upload to AWS Elastic Beanstalk.
